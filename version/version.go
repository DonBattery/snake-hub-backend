package version

// VERSION is Snake-hub backend's version string in the semantic version format
const VERSION = "v0.1.0"

// Get returns Snake-hub backend's version string
func Get() string {
	return VERSION
}
